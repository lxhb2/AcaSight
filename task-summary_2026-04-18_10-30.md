# Task Summary: Dashboard Web Chat API 实现

**Date:** 2026-04-18 10:30 GMT+8
**Status:** ✅ 完成

---

## 用户需求

用户需要一个**Web UI界面**来完成与 Hermes Agent 的交互，包括：
- 工具调用（browser, file, code_execution 等）
- Skill 协助
- 终端命令操作

---

## 问题分析

1. **Hermes CLI 在非交互模式下崩溃**
   - `prompt_toolkit` 库的 Windows 控制台 bug
   - 无法通过 Node.js spawn 调用

2. **Dashboard 原有实现只是静态页面**
   - 直接调用 Ollama API
   - 没有经过 Hermes Agent 的工具链

---

## 解决方案

### 新增 API 端点（api-server.js）

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/chat` | POST | 与 Ollama 对话（非流式） |
| `/api/chat/stream` | POST | 与 Ollama 对话（流式） |
| `/api/hermes/agent` | GET | 获取 Agent 状态（工具、技能列表） |

### Chat API 实现细节

```javascript
// /api/chat 调用 Ollama API
POST /api/chat
{
  "message": "hello",
  "model": "qwen3.5:0.8b"
}

// 响应
{
  "success": true,
  "response": "Hello! How can I help you today? 😊",
  "model": "qwen3.5:0.8b",
  "session_id": "chat_xxx",
  "done": true
}
```

### Dashboard HTML 更新

- API_BASE 改为 `http://localhost:1420`
- Chat 标签页使用新的 `/api/chat` 端点
- 支持模型切换、清空对话、日志记录

---

## 当前服务状态

| 服务 | 端口 | 状态 |
|------|------|------|
| Config Panel API | 1420 | ✅ Running |
| Dashboard Web | 9119 | ✅ Running |
| Ollama API | 11434 | ✅ Running |

---

## 使用方式

### 方式 1: Dashboard Web UI（推荐）
```
1. 打开 http://localhost:9119
2. 选择模型（如 qwen3.5:0.8b）
3. 在输入框输入消息
4. 点击 Send 发送
```

### 方式 2: 直接调用 API
```bash
curl -X POST http://localhost:1420/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"hello","model":"qwen3.5:0.8b"}'
```

---

## 局限性

### 当前实现的局限
- **只能与 Ollama 对话**，无法使用 Hermes 的工具和 Skill
- 需要手动实现工具调用逻辑

### 原因
- Hermes CLI 在非交互模式下崩溃（Windows bug）
- 需要修改 Hermes 源码或使用其他方式（如 ACP 协议）

---

## 未来改进方向

### 方案 A: 修复 Hermes CLI
- 向 Hermes 项目报告 Windows bug
- 添加 `--no-tui` 模式绕过 prompt_toolkit

### 方案 B: 实现 ACP 协议
- 使用 `hermes acp` 启动 Agent 服务器
- Web UI 通过 ACP 协议与 Agent 通信
- 支持完整的工具调用和 Skill 功能

### 方案 C: 直接实现工具调用
- 在 Config Panel API 中实现工具调用逻辑
- 类似 OpenAI Function Calling
- 需要大量开发工作

---

## 文件修改记录

| 文件 | 修改 |
|------|------|
| `H:\HermesPortable\ConfigPanel\api-server.js` | 添加 `/api/chat`, `/api/chat/stream`, `/api/hermes/agent` |
| `H:\HermesPortable\HermesAgent\app\hermes_cli\web_dist\index.html` | API_BASE 改为 localhost:1420 |

---

## 下一步

1. **测试 Web Chat UI** - 打开 http://localhost:9119 验证功能
2. **考虑实现 ACP 协议** - 获取完整的 Agent 能力
3. **或者等待 Hermes 修复** - Windows 非交互模式 bug
