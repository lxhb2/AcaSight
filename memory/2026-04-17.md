# 2026-04-17 Memory Log

## Hermes Portable 配置修复记录

### 问题汇总
1. **18789端口无法访问** - 配置面板错误地尝试打开不存在的Web界面
2. **配置被覆盖** - 运行setup wizard时配置被重置为API模式(SiliconFlow)
3. **本地Ollama配置丢失** - 需要多次恢复custom provider配置

### 修复内容

#### 1. 修复index.html (H:\HermesPortable\ConfigPanel\index.html)
- **问题**: launchHermes()函数尝试打开`http://localhost:18789`，但该端口无服务
- **修复**: 改为显示alert提示，告知用户在新终端窗口中使用Hermes Chat
- **代码变更**:
  ```javascript
  // 旧代码 (错误)
  window.open('http://localhost:18789', '_blank');
  
  // 新代码 (正确)
  alert('Hermes Chat 已在新的终端窗口中启动！\n\n请在弹出的黑色窗口中使用 Hermes。');
  ```

#### 2. 恢复本地Ollama配置
- **Provider**: `custom` (不是`ollama`或`ollama-cloud`)
- **Base URL**: `http://localhost:11434/v1`
- **Model**: `qwen3.5:0.8b`
- **环境变量**: `OPENAI_BASE_URL=http://localhost:11434/v1`

#### 3. 更新启动脚本
- 添加`OPENAI_BASE_URL`环境变量到.bat文件
- 确保`PYTHONIOENCODING=utf-8`避免编码错误

### 正确的工作流程
1. 访问 `http://localhost:1420` - 配置面板
2. 点击"启动Hermes" - 弹出终端窗口
3. 在终端中使用 `hermes chat` 对话
4. **注意**: 18789端口不存在，Hermes Chat是终端应用

### 涉及的文件
- `H:\HermesPortable\ConfigPanel\index.html` - 修复launchHermes函数
- `H:\HermesPortable\ConfigPanel\api-server.js` - 更新provider为custom
- `H:\HermesPortable\HermesAgent\data\.hermes\config.yaml` - 本地Ollama配置
- `H:\HermesPortable\HermesAgent\data\.hermes\.env` - 环境变量
- `H:\HermesPortable\HermesAgent\data\.hermes\launch_chat.bat` - 启动脚本

### 关键发现
- Hermes Chat是**终端应用**，不是Web服务
- 本地Ollama需要使用`custom` provider + `OPENAI_BASE_URL`环境变量
- 配置面板API运行在**1420端口**，不是18789

### 22:32  -  Dashboard系统问题排查

- 完成4项任务：Dashboard按钮集成到配置面板、创建5个快捷脚本、修复Hermes Chat API问题、创建查看配置状态.bat
- 问题A：Dashboard API 生成错误批处理文件（磁盘文件正确但QClaw缓存旧代码，始终生成python而非完整路径）
- 问题B：Dashboard无法启动，_build_web_ui需要npm install && npm run build，web_dist目录不存在
- npm install挂起原因：registry.npmmirror.com国内镜像响应慢；TypeScript被Python typescript包污染（已卸载）
- 解决路径：刷新QClaw缓存解决批处理问题，执行npm install构建Web UI解决Dashboard启动问题

### 22:46  -  Dashboard开发问题分析

- Dashboard 按钮已集成到配置面板
- 已创建5个快捷脚本
- Hermes Chat API 完全正常工作（Python312 路径）
- 查看配置状态.bat 已创建
- 备用 HTML Dashboard 已创建（web_dist/index.html）
- Dashboard API 生成错误批处理文件，磁盘文件代码正确但 QClaw 缓存旧代码
- npm install 一直挂起（registry.npmmirror.com 国内镜像响应慢），导致 Web UI 未构建
- TypeScript 被 pip 的 Python typescript 包污染（已卸载）
- 需要解决 Dashboard API 生成完整路径而非 'python' 的问题
- 需要解决 npm install 挂起问题以完成 Web UI 构建
