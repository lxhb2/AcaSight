# Task Summary: Hermes 配置问题修复

**Date:** 2026-04-18 10:20 GMT+8
**Status:** 🔶 部分完成

---

## 问题分析

用户运行 `hermes chat` 时看到 setup wizard，原因是：

1. **config.yaml 格式不正确** - 原配置文件缺少 `model` 和 `providers` 字段
2. **缺少 API key** - 即使使用本地 Ollama，也需要设置 `OPENAI_API_KEY`（可以是任意值）
3. **缺少 OPENAI_BASE_URL** - 需要指向 Ollama 的 API 端点

---

## 已完成的修复

### 1. 更新 config.yaml
```yaml
# 之前
model_provider: auto

# 之后
model: qwen3.5:0.8b
providers:
  local_ollama:
    type: ollama
    base_url: http://localhost:11434
```

### 2. 更新 .env
```bash
# 新增
OPENAI_API_KEY=ollama
OPENAI_BASE_URL=http://localhost:11434/v1
```

### 3. 验证配置
```
hermes status:
  Model:        qwen3.5:0.8b ✅
  Provider:     Custom endpoint ✅
  OpenAI        ✓ *** ✅
```

---

## 剩余问题

### Windows 控制台问题
运行 `hermes chat -q "hello"` 时出错：
```
prompt_toolkit.output.win32.NoConsoleScreenBufferError: No Windows console found.
```

**原因：** Hermes 使用 `prompt_toolkit` 库，在非交互式终端中会崩溃。

**解决方案：**
1. **方案 A**: 在真实终端中运行（双击 `启动 Hermes.bat`）
2. **方案 B**: 使用 Dashboard Web UI（http://localhost:9119）
3. **方案 C**: 修改 Hermes 源码检测非交互模式并跳过 TUI

---

## 当前可用功能

| 功能 | 状态 | 访问方式 |
|------|------|----------|
| Dashboard Web | ✅ 正常 | http://localhost:9119 |
| Chat (Web UI) | ✅ 正常 | Dashboard → Chat tab |
| Ollama API | ✅ 正常 | http://localhost:11434 |
| Hermes CLI (终端) | ⚠️ 需要真实终端 | 双击 `启动 Hermes.bat` |
| Hermes CLI (非交互) | ❌ Windows bug | 需要源码修复 |

---

## 推荐使用方式

### 方式 1: Dashboard Web (推荐)
```
1. 启动 Dashboard: 运行 dashboard_server.py
2. 访问: http://localhost:9119
3. 在 Chat tab 中选择模型并对话
```

### 方式 2: 终端模式
```
1. 双击 H:\HermesPortable\启动 Hermes.bat
2. 在新窗口中输入消息
```

---

## 文件修改记录

| 文件 | 修改 |
|------|------|
| `config.yaml` | 添加 model, providers 字段 |
| `.env` | 添加 OPENAI_API_KEY, OPENAI_BASE_URL |
| `dashboard_server.py` | 已创建（昨日） |
| `web_dist/index.html` | 已增强（昨日） |

---

## 下一步建议

1. **用户操作**: 在真实终端中运行 Hermes（双击 `启动 Hermes.bat`）
2. **可选**: 向 Hermes 项目报告 Windows 非交互模式的 bug
3. **可选**: 添加辅助 LLM provider（如 OpenRouter）以启用 context compression
