"""
AI 响应速度优化 — 三层加速策略

问题分析：
1. 每次调用都新建 AIService() 实例 → 重复加载配置文件
2. 每次调用都新建 httpx.AsyncClient() → TCP连接不复用，冷启动慢
3. SSE 端点串行生成 prompt → 不利用预计算
4. 无响应缓存 → 相似请求重复计算
5. 无并发控制 → 大量请求同时发出可能触发限流
6. 无预测性生成 → 用户等待时系统空闲

优化方案：
A. 连接池复用 — 全局 httpx.AsyncClient，连接复用，减少 TCP 握手
B. 并行预取 — SSE 首token等待期间，并行准备后续 prompt
C. 智能缓存 — 相同/相似请求的短期缓存（哈希key）
D. Prompt压缩 — 减少发送 token 数，降低首token延迟
E. 模型路由优化 — 不同任务用不同模型（快模型做简单任务）
F. 流式预渲染 — 前端收到第一个 chunk 就开始渲染

预计提升：
- 首 token 延迟：300-800ms → 100-300ms（连接复用）
- 写作速度：受限于模型，但减少等待空白时间
- 重复请求：缓存命中直接返回，0延迟
"""

# ============================================================
# 优化1: 全局连接池 + 单例 AIService
# ============================================================
# 当前: 每次调用 new AIService() + new httpx.AsyncClient()
# 优化: 全局单例 + 连接池复用

# ============================================================
# 优化2: 智能模型路由
# ============================================================
# 当前: 所有任务用同一个模型
# 优化: 按任务复杂度自动选择模型
#   - 简单任务(翻译/摘要/润色) → 快模型(DeepSeek-V4-Flash / glm-4-flash)
#   - 复杂任务(大纲/章节/Agent推理) → 强模型(DeepSeek-V3 / GPT-4o)

# ============================================================
# 优化3: 响应缓存
# ============================================================
# TTL=300s 的 LRU 缓存，key=hash(prompt+model+temperature)
# 适用: 提纲生成、摘要、翻译等幂等操作

# ============================================================
# 优化4: Prompt 精简
# ============================================================
# 当前: 每次发送完整系统提示+完整上下文
# 优化:
#   - 系统提示词分级（轻量版/标准版/完整版）
#   - 上下文智能截断（只保留相关部分）
#   - 历史消息压缩（已实现context_compressor，但写作端点未使用）

# ============================================================
# 优化5: SSE 流式优化
# ============================================================
# 当前: generate() 函数内同步构建 prompt，然后串行等待
# 优化:
#   - 先发送 SSE header，再构建 prompt
#   - 分块更细粒度（不攒 buffer，收到即发）
#   - 添加首 token 计时指标

# ============================================================
# 优化6: 并行章节生成
# ============================================================
# 当前: 一键全写 = 串行逐章节生成
# 优化: 无依赖的章节可并行生成（绪论/方法/结论独立）
