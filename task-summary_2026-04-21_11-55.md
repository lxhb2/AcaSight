# 任务总结 2026-04-21_11-55

## 目标
继续修复 Web UI v2 的流式聊天功能

## 关键问题与修复

### 问题 1：`_get_projects_dir()` 路径错误
- **现象**：`load_model_config()` 读不到 config/model_config.json
- **原因**：`os.path.dirname(os.path.dirname(__file__))` 往上2级 = `src/`，不是 `projects/`
- **修复**：改为往上3级：`os.path.dirname(os.path.dirname(os.path.dirname(__file__)))`

### 问题 2：SSE generator 输出编码错误（Windows GBK）
- **现象**：Flask Response generator 中的 `yield f"data: ..."` 在 GBK 环境下崩溃
- **原因**：Windows 控制台默认 GBK，Python 字符串包含 emoji/中文时编码失败
- **修复**：所有 SSE yield 改为 `.encode('utf-8')` 输出 bytes；抽取 `_sse_chunk()` 和 `_sse_done()` 辅助函数

### 问题 3：Flask session 在 generator 延迟执行时不可用
- **现象**：`Working outside of request context` 错误
- **原因**：SSE Response 的 generator 在 request context 结束后才执行，`session` proxy 失效
- **修复**：在 request context 内提前读取 session 数据，传递给 `_do_llm_stream()` 函数

### 问题 4：`is_online()` 使用 `socket.setdefaulttimeout()` 
- **现象**：全局超时设置影响其他 socket 操作
- **修复**：改用 `sock.settimeout()` 替代 `socket.setdefaulttimeout()`

## 验证结果

| 测试项 | 状态 |
|--------|------|
| `/api/status` 即时返回 | ✅ |
| 快速命令（查看项目）即时返回 | ✅ |
| LLM 流式聊天（SiliconFlow GLM-5.1）| ✅ 475 chars |
| Emoji/中文正常显示 | ✅ |
| 状态缓存后台线程无超时 | ✅ |

## 当前运行状态
- Web UI v2.1 运行在 http://localhost:5050
- Provider: siliconflow / Pro/zai-org/GLM-5.1
- Ollama 可用 (qwen3.5:4b)
- LM Studio 可用
