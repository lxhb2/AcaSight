# 任务摘要 2026-04-17 22:56

## 完成工作

### P0 阻塞问题已解决
- ✅ **通道阻塞修复**：execPromise（2分钟同步）→ execFast（5秒异步）+ Promise.all 并发
- ✅ **curl 替换**：Windows 自带 PowerShell TcpClient.Connect() 替代 curl
- ✅ **验证**：5 端点并发测试通过（8秒，历史记录：单端点可达 2 分钟）

### P1 本地 Ollama Provider 已配置
- ✅ config.yaml providers: {} → local_ollama provider
- ✅ 默认模型：minimax-m2.7 → qwen3.5:0.8b（本地）
- ✅ /api/status 正确显示 provider: local_ollama, model: qwen3.5:0.8b

## 代码改动

### api-server.js 改动（最小改动原则）
1. **新增 execFast()** - 非阻塞异步 exec，5秒超时，不锁事件循环
2. **新增 execBg()** - fire-and-forget 后台执行
3. **handleStatus()** - 新增轻量级状态端点（PowerShell 端口检测）
4. **handleEnvCheck()** - 替换 execPromise → execFast + Promise.all
5. **handleOllamaStatus()** - 替换 execPromise → execFast + PowerShell TcpClient
6. **handleHermesInstall()** - execPromise → execBg（fire-and-forget）
7. **Ollama installer 路径** - 修正多路径检测

### config.yaml 改动
- providers: {} → local_ollama + ollama-cloud 双 provider
- model.provider: ollama-cloud → local_ollama
- model.default: minimax-m2.7 → qwen3.5:0.8b
- model_provider: api → local

## 技术发现
- npm --prefix 在当前 PowerShell 环境无效，始终从 workspace 读取
- PowerShell Invoke-RestMethod 不支持 -AsJob，需用 Start-Job
- Python HTTP server with serve_forever() 在 Start-Process -NoNewWindow 下会提前退出
- curl 在 Windows 上行为不一致，改用 PowerShell 原生 TcpClient