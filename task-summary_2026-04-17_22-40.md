# 任务摘要 2026-04-17 22:40

## 完成的 4 个任务

### ✅ 1. Dashboard 按钮集成到配置面板
- `H:\HermesPortable\ConfigPanel\index.html` 已添加 Dashboard 启动按钮
- 包含"启动 Dashboard"、"打开 Dashboard Web界面"、编辑配置文件等快捷操作

### ✅ 2. 创建快捷脚本
5个批处理文件已创建：
- `启动全部服务.bat` - 一键启动所有服务
- `启动 Dashboard.bat` - 快速启动 Dashboard
- `启动 Hermes.bat` - 启动 Hermes Chat
- `启动配置面板.bat` - 启动配置面板
- `查看配置状态.bat` - 命令行显示配置

### ✅ 3. 汉化程序
- Hermes Dashboard **自带完整中文** (`zh.ts`)，无需额外制作
- Config Panel 已是中文界面

### ✅ 4. 命令行配置可视化
- `查看配置状态.bat` 显示完整配置信息
- Config Panel 包含配置文件查看器

## 核心修复

### 🔧 Dashboard API 修复（根本原因）
**问题**: QClaw 旧进程 (PID 8452) 缓存了旧版 `api-server.js`，导致 Dashboard 批处理生成 "python" 而非完整路径。

**解决**: QClaw 重启后自动加载正确版本。Dashboard API 现返回 200 并生成正确的批处理文件（Python312 完整路径）。

### 🌐 备用 Dashboard HTTP 服务器
**问题**: `hermes dashboard` 命令需要 npm build 构建 Web UI，但 npm install 在 H: 盘环境下一直挂起。

**解决**: 创建了 `H:\HermesPortable\dashboard_server.py` - 一个独立的 Python HTTP 服务器，直接服务备用 HTML Dashboard（自包含，无需外部资源）。

## 当前服务状态

| 服务 | 端口 | 状态 |
|------|------|------|
| Dashboard HTTP | 9119 | ✅ 运行中 |
| Config Panel | 1420 | ✅ 运行中 |
| Ollama | 11434 | ✅ 运行中 |
| Hermes Chat | 18080 | 待启动 |

- Dashboard HTML: http://localhost:9119
- Config Panel: http://localhost:1420
- Hermes Chat: 通过 `启动 Hermes.bat` 或 Config Panel 启动

## 文件变更

- `H:\HermesPortable\ConfigPanel\index.html` - 添加 Dashboard 按钮
- `H:\HermesPortable\ConfigPanel\api-server.js` - 添加 Dashboard/配置文件 API
- `H:\HermesPortable\HermesAgent\app\hermes_cli\web_dist\index.html` - 备用 HTML Dashboard
- `H:\HermesPortable\HermesAgent\app\hermes_cli\web_dist\assets\` - 空目录（FastAPI 需要）
- `H:\HermesPortable\dashboard_server.py` - 备用 Python HTTP 服务器

## 待处理

1. **Hermes Chat (18080)** - 需要手动启动
2. **Dashboard Web UI 完整版** - 需要 npm build 解决（H盘长路径/npm 挂起问题）
   - 解决方向：npm --prefix 始终从 workspace 而非 --prefix 目录读取 package.json
   - 备选：手动构建 HTML 或使用 yarn/pnpm

## 技术笔记

- npm install 在 H: 盘挂起的根本原因：npm --prefix 参数不起作用，始终从 workspace 目录读取
- api-server.js 中 `_BAT` 模板使用硬编码完整路径（Python312）
- QClaw 重启后自动重新加载 api-server.js（新版本）
