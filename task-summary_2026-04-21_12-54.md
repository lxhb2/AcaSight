# 任务总结 2026-04-21_12-54

## 问题
Agent 没有上下文记忆功能，无法识别上文提出的问题给它的回答。

**现象**:
- 用户: "1A,2A,3A,4.短期想去漏洞盒子挖低危漏洞赚钱"
- AI: "另外，你提到的 "1A,2A,3A,4" 是什么意思呀？"

AI 完全忘记了自己上一轮提出的四个问题。

## 根因分析

### 问题 1: Flask session 在 generator 延迟执行时不可用
- SSE Response 的 generator 在 request context 结束后才执行
- `session` proxy 在 context 外部访问时报错 "Working outside of request context"
- 之前注释掉了历史保存逻辑

### 问题 2: 前端 `/api/save_history` 调用不可靠
- 异步 fetch 可能在页面刷新前未完成
- 没有重试机制

## 解决方案

### 核心改动: 文件存储历史记录

不再依赖 Flask session，改用文件存储：

```python
_CHAT_HISTORY_FILE = os.path.join(PROJECTS_DIR, 'data', 'chat_history.json')


def _load_chat_history():
    """从文件加载历史"""
    if os.path.exists(_CHAT_HISTORY_FILE):
        with open(_CHAT_HISTORY_FILE, 'r', encoding='utf-8') as f:
            return json.load(f)
    return []


def _save_chat_history(history):
    """保存历史到文件"""
    with open(_CHAT_HISTORY_FILE, 'w', encoding='utf-8') as f:
        json.dump(history, f, ensure_ascii=False, indent=2)
```

### API 改动

| 接口 | 功能 |
|------|------|
| `POST /api/save_history` | 保存对话到文件 |
| `GET /api/history` | 获取对话历史 |
| `POST /api/history/clear` | 清除对话历史 |

### 前端改动

流式响应 `[DONE]` 时调用保存：

```javascript
if(dataStr.trim() === '[DONE]') {
    if(fullContent) {
        fetch('/api/save_history',{
            method:'POST',
            headers:{'Content-Type':'application/json'},
            body:JSON.stringify({user:msg, assistant:fullContent})
        }).catch(()=>{});
    }
}
```

## 验证结果

| 测试 | 结果 |
|------|------|
| User: "我叫张三，今年25岁" | ✅ AI 响应 491 chars |
| 保存历史 | ✅ 成功 |
| User: "我叫什么名字？" | ✅ AI 知道 "张三" |

**上下文记忆功能完全修复！**
